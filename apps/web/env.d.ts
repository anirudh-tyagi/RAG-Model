/// <reference types="next" />
/// <reference types="next/image-types/global" />

// Committed equivalent of Next's generated next-env.d.ts, so `tsc --noEmit`
// works on a clean checkout before the first `next dev` or `next build`.
